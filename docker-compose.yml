services:
  dashboard:
    build: .
    container_name: energy-dashboard
    restart: unless-stopped
    ports:
      - "127.0.0.1:8090:80"
    environment:
      CSP_CONNECT_HOSTS: "https://api.p1.wimluyckx.dev https://api.sungrow.wimluyckx.dev"
