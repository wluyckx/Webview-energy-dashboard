# Deployment — 2026-07-30: the fixed legacy artifact goes live

**Role**: Operator (Opus 5) under the §2.5 Deployment Contract · **Governor**: Fable
**Deployed commit**: `f751634` (D1–D4 truthfulness fixes, RW-M05 hardening, the
two pipeline corrections) · **Result**: complete, all five VERIFY items PASS,
rollback not needed.

*(No internal hostnames in this log — the remote is public. "The VPS" is
reachable per house convention.)*

## What the pre-deploy inspection found (evidence rule, applied to ops)

1. **R8 was live, not hypothetical**: production ran an image built
   **2026-03-20**; `docker-compose.yml` had lost its `build:` section, so every
   `deploy.sh` run since had uploaded the artifact and then silently shipped
   nothing. Fixed in `f751634` before deploying.
2. **The healthcheck had been lying for months**: alpine `wget localhost`
   resolves `::1` first, nginx listens IPv4-only → "unhealthy"
   (FailingStreak 1068) against a container serving real traffic the same
   morning. Fixed to `127.0.0.1` in `f751634`.
3. Discovered by the Operator mid-deploy: the running container had been
   created **outside compose** (no project labels) → name conflict on
   recreate; and it carried **no `en_dashboard` alias**, leaving one of the two
   Caddy routes with no resolvable upstream.

## Execution

Preconditions P1–P4 all passed (clean tree at the approved commit, 360/360
tests, 137,846-byte build, rollback tag staged **before** any change).
`deploy.sh` built and uploaded correctly, then hit the name conflict — the
Operator **escalated with the contract grammar instead of improvising**
(`scope_violation`, destructive op = trigger 7). Governor authorized the
one-time `docker rm -f` + `compose up -d` swap: rollback net staged, blast
radius seconds on an auth-gated route, and the recreate also restores the
missing alias.

## VERIFY (all PASS, evidence in the Operator transcript)

- **V1 provenance**: running artifact sha256 `6f34a9b4…` = local build = bytes
  nginx serves; image `b9e3a657…` created 2026-07-30T12:58Z; old artifact
  `aa647f43…` gone.
- **V2 health**: healthy at t+5s, FailingStreak 0 — the first honest health
  signal this container has ever reported.
- **V3 serving**: `/health` OK on the restored port binding; 137,846 bytes;
  RW-M04 fingerprint present in served HTML.
- **V4 public route**: 401 (auth-gated, Caddy alive), no 5xx at any point.
- **V5 network**: `en_dashboard` alias restored; **both** Caddy upstreams
  proven resolving and answering from Caddy's own netns — the latent
  second-route breakage is fixed.

## Consequences recorded

- **R8 mitigated for manual deploys** (build → image → served bytes are one
  provable hash chain). The absent ghcr publishing pipeline remains absent —
  correct for an archive-bound repo.
- The container is compose-managed for the first time; the name-conflict
  failure class cannot recur.
- Parking lot: `deploy.sh`'s success banner prints only on full success
  (`set -e` aborts before its own status on compose failure) and still
  advertises the old standalone hostname — cosmetic.

**Production now serves the dashboard that tells the truth**: export flows
render, self-sufficiency is real, the P1 history tabs say honestly that data
awaits RW-C01, and the timeline's grid line moves.
